import { Body, Controller, Get, Param, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { CurrentUser } from '../../core/auth/decorators/current-user.decorator'; import { JwtAuthGuard } from '../../core/auth/guards/jwt-auth.guard'; import { JwtPayload } from '../../core/auth/interfaces/jwt-payload.interface'; import { Permissions } from '../../core/permissions/decorators/permissions.decorator'; import { PermissionsGuard } from '../../core/permissions/guards/permissions.guard'; import { CreateBomDto } from './dto/create-bom.dto'; import { CreateProductionOrderDto } from './dto/create-production-order.dto'; import { ProductionService } from './production.service';
@ApiTags('Production / PCP') @ApiBearerAuth('bearer') @UseGuards(JwtAuthGuard,PermissionsGuard) @Controller('production') export class ProductionController { constructor(private readonly s:ProductionService){} @Post('boms') @Permissions('production.bom.create') createBom(@CurrentUser()u:JwtPayload,@Body()d:CreateBomDto){return this.s.createBom(u.companyId,d)} @Get('boms') @Permissions('production.bom.read') boms(@CurrentUser()u:JwtPayload){return this.s.listBoms(u.companyId)} @Post('orders') @Permissions('production.orders.create') createOrder(@CurrentUser()u:JwtPayload,@Body()d:CreateProductionOrderDto){return this.s.createOrder(u.companyId,d)} @Get('orders') @Permissions('production.orders.read') orders(@CurrentUser()u:JwtPayload){return this.s.listOrders(u.companyId)} @Get('orders/:id') @Permissions('production.orders.read') order(@CurrentUser()u:JwtPayload,@Param('id')id:string){return this.s.getOrder(u.companyId,id)} @Post('orders/:id/start') @Permissions('production.orders.start') start(@CurrentUser()u:JwtPayload,@Param('id')id:string){return this.s.start(u.companyId,id)} @Post('orders/:id/complete') @Permissions('production.orders.complete') complete(@CurrentUser()u:JwtPayload,@Param('id')id:string){return this.s.complete(u.companyId,id)} @Post('orders/:id/cancel') @Permissions('production.orders.cancel') cancel(@CurrentUser()u:JwtPayload,@Param('id')id:string){return this.s.cancel(u.companyId,id)} }
