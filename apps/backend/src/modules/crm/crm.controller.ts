import { Body, Controller, Get, Param, Patch, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger';
import { CurrentUser } from '../../core/auth/decorators/current-user.decorator'; import { JwtAuthGuard } from '../../core/auth/guards/jwt-auth.guard'; import { JwtPayload } from '../../core/auth/interfaces/jwt-payload.interface'; import { Permissions } from '../../core/permissions/decorators/permissions.decorator'; import { PermissionsGuard } from '../../core/permissions/guards/permissions.guard'; import { CrmService } from './crm.service'; import { CreateActivityDto,CreateLeadDto,CreateOpportunityDto,ConvertLeadDto } from './dto/crm.dto';
@ApiTags('CRM') @ApiBearerAuth('bearer') @UseGuards(JwtAuthGuard,PermissionsGuard) @Controller('crm') export class CrmController { constructor(private s:CrmService){} @Post('leads') @Permissions('crm.leads.create') lead(@CurrentUser()u:JwtPayload,@Body()d:CreateLeadDto){return this.s.createLead(u.companyId,d)} @Get('leads') @Permissions('crm.leads.read') leads(@CurrentUser()u:JwtPayload){return this.s.listLeads(u.companyId)} @Post('leads/:id/convert') @Permissions('crm.leads.convert') convert(@CurrentUser()u:JwtPayload,@Param('id')id:string,@Body()d:ConvertLeadDto){return this.s.convertLead(u.companyId,id,d)} @Post('opportunities') @Permissions('crm.opportunities.create') opp(@CurrentUser()u:JwtPayload,@Body()d:CreateOpportunityDto){return this.s.createOpportunity(u.companyId,d)} @Get('opportunities') @Permissions('crm.opportunities.read') opps(@CurrentUser()u:JwtPayload){return this.s.listOpportunities(u.companyId)} @Post('activities') @Permissions('crm.activities.create') act(@CurrentUser()u:JwtPayload,@Body()d:CreateActivityDto){return this.s.createActivity(u.companyId,u.sub,d)} @Patch('activities/:id/complete') @Permissions('crm.activities.update') done(@CurrentUser()u:JwtPayload,@Param('id')id:string){return this.s.completeActivity(u.companyId,id)} }
